#映射文件地址
mybatis:
  mapper-locations: classpath:mapper/*.xml
#配置数据源
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/manager?serverTimezone=GMT&useSSL=false&useUnicode=true&characterEncoding=UTF8
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

#sql日志
logging:
  level:
    com.manager.admin.mapper : debug