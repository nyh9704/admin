<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.manager.admin.mapper.MenuMapper">

    <!-- 获取全部菜单 -->
    <select id="listMenus" resultType="com.manager.admin.entity.MenuEntity">
        select menu_id menuId,menu_name menuName,root_id rootId from sys_menu
    </select>
</mapper>